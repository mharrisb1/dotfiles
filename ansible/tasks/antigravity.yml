---
- name: Install Antigravity CLI
  shell: curl -fsSL https://antigravity.google/cli/install.sh | bash
  args:
    creates: "{{ user_home }}/.local/bin/agy"
  when: install_antigravity | default(false) | bool

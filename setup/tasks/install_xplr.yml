---
- name: Clone xplr
  git:
    repo: https://github.com/sayanarijit/xplr.git
    dest: "{{ user.home }}/xplr"
    depth: 1
    update: false

- name: Build xplr
  shell: "{{ user.home }}/.cargo/bin/cargo build --locked --release --bin xplr"
  args:
    chdir: "{{ user.home }}/xplr"
    creates: "{{ user.home }}/xplr/target/release/xplr"

- name: Install xplr binary
  become: true
  copy:
    src: "{{ user.home }}/xplr/target/release/xplr"
    dest: /usr/local/bin/xplr
    mode: "0755"
    remote_src: true
